include variables.pug
include settings.pug

doctype transitional
html
    head
        block meta_data
            include partials/head/meta_data.pug
        block client_styles
            include partials/head/client_styles.pug
        block custom_styles
            include partials/head/custom_styles.pug
        include partials/head/mso_conditionals.pug
    body(
        leftmargin="0"
        marginwidth="0"
        topmargin="0"
        marginheight="0"
        offset="0"
    )
        center
            +table(id="bgTable&attributes"): tr: td(style="padding: 15px;")
                +table(width=emailWidth): tr: td
                    +table
                        tr
                            block preheader
                        tr
                            block hero_image
                        tr
                            block content_one
                        tr
                            block content_two
            +gmail_hack
